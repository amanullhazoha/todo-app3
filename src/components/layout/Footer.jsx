import classes from '../css/footer.module.css';

const Footer = () => (
    <footer id={classes.footer}>
        <p>React Project Todo Application</p>
    </footer>
);

export default Footer;
